pipeline {
    any{
        stages {
            stage('pull') {
                steps {
                    git clone
                }
            }
            stage('build') {
                steps {
                   mvn clean
                }
            }
        }
    }
}